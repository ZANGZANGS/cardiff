server.port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cardiff
    username: devuser
    password: 1q2w3e4r
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  thymeleaf:
    cache: false

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

server:
  servlet:
    session:
      timeout: 30m
      tracking-modes: cookie #JSESSIONID URL 노출되지 않도록 설정